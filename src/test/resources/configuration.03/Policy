
Policy (
   Allow [
	 User fergerson;
     Group administrator;
   ] to write ;
   Allow [
     Group administrator;
   ] to read ;
) on /pizza-fergerson.history ;

Policy (
  Allow [
    User redmond;
  ] to read;
  Allow [
    User redmond;
  ] to write;
) on /redmonds-private-directory ;

