package org.protege.owl.server.api;

/**
 * Represents a change history where the end revision is exactly one revision
 * later than the original revision.
 *
 * @author redmond
 */
public interface SingletonChangeHistory extends ChangeHistory {
    // NO-OP
}
